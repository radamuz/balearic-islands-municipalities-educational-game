---
description: Commit staged changes and push to the remote
---

Mira los cambios en staging (`git status` y `git diff --staged`), crea un commit con un mensaje descriptivo siguiendo el estilo de los commits recientes del repo, y haz `git push`.

Si no hay cambios en staging, avisa al usuario en vez de hacer un commit vacío.
